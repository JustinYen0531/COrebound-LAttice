{
  "name": "cola-remake",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "COrebound LAttence: Remake Edition — standalone, fully completable single-player build",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "node -e \"require('esbuild').buildSync({entryPoints:['test/e2e.ts'],bundle:true,platform:'node',format:'cjs',outfile:'test/.e2e.cjs',logLevel:'warning'})\" && node test/.e2e.cjs"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
